{
    "Print": {
      "prefix": "print",
      "body": ["Print(\"$1\");"],
      "description": "Вывод в консоль"
    },
    "Timer": {
      "prefix": "timer",
      "body": [
        "Timer myTimer = new Timer(CALL_CATEGORY_SYSTEM);",
        "myTimer.Run(5.0, this, \"$1\", null, true);"
      ],
      "description": "Создание таймера"
    },
    "If Statement": {
      "prefix": "if",
      "body": [
        "if ($1) {",
        "\t$2",
        "}"
      ],
      "description": "Базовая структура if"
    },
    "For Loop": {
      "prefix": "for",
      "body": [
        "for (int i = 0; i < $1; i++) {",
        "\t$2",
        "}"
      ],
      "description": "Базовая структура for"
    }
  }
  